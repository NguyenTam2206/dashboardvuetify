import Vue from 'vue';
import Vuetify from 'vuetify/lib';
Vue.use(Vuetify);

export default new Vuetify({
    theme: {
        // primary : '#',
        // success : '#',
        // error : '#',
        // warning : '#',
        // info : '#'
    }
});
//set brand color tại đây (to do...)
//https://materialdesignicons.com/
